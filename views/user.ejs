<!doctype html>
<html>
  <head>
    <title>Users</title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
  </head>
  <body>
    <h1>Users</h1>
    <ul>
      <% user.forEach(function(user) { %>
       <li><%= user.text %> <%= user.name %></li>
        <br>
        <% }) %>
      </ul>
  </body>
</html>

